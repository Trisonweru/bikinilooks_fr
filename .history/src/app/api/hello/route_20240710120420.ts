import { NextResponse } from 'next/server';

export async function GET() {
   try {
      let result: any = {}
      const isCached = false

      

    } catch (err: any) {
      NextResponse..json({ status: 400, message: err })
    }
}
